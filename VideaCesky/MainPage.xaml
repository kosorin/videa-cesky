<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VideaCesky"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sample="using:Blend.SampleData.SampleDataSource"
    x:Class="VideaCesky.MainPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" 
    d:DataContext="{d:DesignData /SampleData/SampleDataSource/SampleDataSource.xaml}">

    <Page.Resources>
        <sample:SampleDataSource x:Key="SampleDataSource" d:IsDataSource="True"/>

        <local:DateTimeConverter x:Key="DateTimeConverter" />
        <local:BoolNegationToVisibilityConverter x:Key="BoolNegationToVisibilityConverter" />
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />


        <Style x:Key="TitleStyle" BasedOn="{StaticResource SubheaderTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource VideoColorBrush}"/>
        </Style>

        <Style x:Key="DateStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource PhoneDisabledBrush}"/>
            <Setter Property="FontFamily" Value="{ThemeResource PhoneFontFamilyNormal}"/>
            <Setter Property="FontSize" Value="{ThemeResource TextStyleExtraLargeFontSize}"/>
            <Setter Property="TextWrapping" Value="WrapWholeWords"/>
            <Setter Property="LineHeight" Value="33.5"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="Opacity" Value="0.95"/>
        </Style>

        <Style x:Key="DetailStyle" BasedOn="{StaticResource BodyTextBlockStyle}" TargetType="TextBlock" >
            <Setter Property="FontFamily" Value="{ThemeResource PhoneFontFamilyNormal}"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="TextWrapping" Value="WrapWholeWords"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="Opacity" Value="0.95"/>
        </Style>
    </Page.Resources>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="nápověda" Icon="Help" Click="AppBarButton_Click" />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>



        <Grid x:Name="ContentRoot" Margin="20,0">
            <ScrollViewer>
                <StackPanel>
                    <TextBlock Text="Videa Česky" Margin="0,20,0,20" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>

                    <ListView ItemsSource="{Binding VideoList}" SelectionChanged="ListView_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,20">

                                    <TextBlock Text="{Binding Title}" Style="{ThemeResource TitleStyle}" />
                                    <TextBlock Text="{Binding Date, Converter={StaticResource DateTimeConverter}}" Style="{ThemeResource DateStyle}" />

                                    <Border Margin="0,10" BorderThickness="1" BorderBrush="{ThemeResource PhoneChromeBrush}" VerticalAlignment="Top" Grid.ColumnSpan="2">
                                        <Image Source="{Binding ImageUri}" />
                                    </Border>

                                    <TextBlock Text="{Binding Detail}" Style="{ThemeResource DetailStyle}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <Grid Margin="0,20,0,40">
                        <TextBlock Visibility="{Binding IsError, Converter={StaticResource BoolToVisibilityConverter}}" Text="Něco se pokazilo :(" Style="{ThemeResource SubheaderTextBlockStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <Grid Visibility="{Binding CanLoadMore, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Button Visibility="{Binding Loading, Converter={StaticResource BoolNegationToVisibilityConverter}}" Tapped="LoadMoreButton_Tapped" Content="Nahrát víc..." HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <ProgressRing IsActive="{Binding Loading}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
